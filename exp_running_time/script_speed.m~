% graph = 1;
% fprintf('Graph %d\n', graph);
% main_speed;
% save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
% fprintf('\n');
% 
% graph = 2;
% fprintf('Graph %d\n', graph);
% main_speed;
% save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
% fprintf('\n');

% graph = 3;
% fprintf('Graph %d\n', graph);
% main_speed;
% save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
% fprintf('\n');

graph = 4;
fprintf('Graph %d\n', graph);
main_speed;
save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
fprintf('\n');

% graph = 5;
% fprintf('Graph %d\n', graph);
% main_speed;
% save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
% fprintf('\n');

% graph = 6;
% fprintf('Graph %d\n', graph);
% main_speed;
% save(['results_running_time/G' num2str(graph) '_' num2str(instance) '.mat'], 't0', 'time_U_SR', 'time_S_U', 'time_L_4', 'time_L_6', 'time_L_8');
% fprintf('\n');
